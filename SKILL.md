---
name: bank-reconciliation
description: 銀行匯款對帳技能（全域可用）。把官網後台匯出的「匯款訂單」CSV（cp950）整理進銀行匯款對帳總表：交易日（匯款日期）、存款金額（匯款金額）、摘要（轉帳後五碼，遇賣家備註寫「正確後5碼」時改用正確碼覆蓋）、備註（匯款銀行）、部門（固定電圖部）、網站訂單編號、賣家備註；摘要自動以文字格式保留前導零。說「銀行對帳」「匯款對帳」「整理匯款訂單」「整理銀行資料表」「匯款資料整理」時載入。
---

# 銀行匯款對帳 (bank-reconciliation)

## 用途

把官網後台（綠界／藍新金流）匯出的**匯款訂單 CSV**，整理進**銀行匯款對帳總表**（xlsx），供後續與銀行對帳單核對。

## 檔案位置

`~/.config/opencode/skills/bank-reconciliation/bank_recon.py`

## 使用方式

```powershell
python ~/.config/opencode/skills/bank-reconciliation/bank_recon.py <匯款訂單.csv> [--total 對帳總表.xlsx] [--dept 電圖部]
```

### 參數

| 參數 | 說明 |
|------|------|
| `匯款訂單.csv` | 官網匯出的匯款訂單 CSV（cp950 / Big5） |
| `--total` | 對帳總表檔名（預設 `銀行匯款對帳資料-總表.xlsx`），**不存在會自動建立** |
| `--dept` | 部門固定值（預設 `電圖部`） |
| `--overwrite` | 清空總表既有資料列後重寫（只保留表頭） |

### 範例

```powershell
python ~/.config/opencode/skills/bank-reconciliation/bank_recon.py "E-0922.csv"
python ~/.config/opencode/skills/bank-reconciliation/bank_recon.py "E-0922.csv" --total "銀行匯款對帳資料-總表.xlsx"
```

---

## 總表欄位（7 欄）

`交易日 | 存款金額 | 摘要 | 備註 | 部門 | 網站訂單編號 | 賣家備註`

## 欄位對應規則（定案）

| 總表欄位 | 來源／規則 |
|----------|-----------|
| 交易日 | 官網「匯款日期」（`YYYY-MM-DD`），輸出為日期格式 `yyyy/mm/dd`；未匯款者留空 |
| 存款金額 | 官網「匯款金額」（數字格式 `#,##0`）；未匯款者為 0 |
| 摘要 | 官網「轉帳後五碼」；**若「賣家備註」含「正確後5碼」等字樣，改用該 5 位數字覆蓋**。一律存成**文字格式**（保留 `00780`、`08986` 前導零） |
| 備註 | 官網「匯款銀行」原文（例：`中國信託`、`中華郵政`、`013`、`郵局`） |
| 部門 | 固定值（預設 `電圖部`） |
| 網站訂單編號 | 官網「訂單編號」（E 開頭，例：`E2609080034`） |
| 賣家備註 | 官網「賣家備註」原文 |

> 來源 CSV 沒有的欄位（例如早期版本的「銷貨單號」「客編」）一律**保留空白**。

---

## 處理流程

1. **讀取 CSV**：依序嘗試 `cp950` → `big5` → `utf-8-sig` → `utf-8`，自動判斷編碼
2. **刪除全空白列**
3. **逐列對應 7 欄**（規則如上表）
4. **摘要修正**：掃描「賣家備註」，若有「正確」字樣則抓出第一個 5 位數字覆蓋摘要
   - 例：`轉帳後五碼=35538`、`賣家備註=正確後5碼06306` → 摘要輸出 `06306`
5. **寫入總表**：讀入既有總表 → 於最後一列之後**接續寫入**（表頭與既有資料、參考列都保留）
6. **格式化**：交易日置中＋日期格式、存款金額靠右＋千分位、摘要文字格式＋置中

---

## 注意事項

1. **來源 CSV 別用 Excel 開啟後另存**：12 位數的單號（如出貨單號）會被 Excel 轉成科學記號 `9.07873E+11`，末幾碼永久遺失。要改請用文字編輯器或直接重新匯出。
2. **摘要是文字格式**：即使 `24768` 這種沒有前導零的值也存成文字，避免 Excel 自動轉數字。
3. **總表可累積**：每次執行都接在最後一列之後，適合每天把新一批匯款資料併入同一張總表。
4. **未匯款列**（匯款方式＝未選擇匯款方式、匯款金額 0）：交易日／摘要／備註留空、存款金額 0，需人工確認。
5. 官網 CSV 欄位名若改版，需同步調整 `bank_recon.py`。

---

## 依賴

- `openpyxl`

```powershell
pip install openpyxl
```
