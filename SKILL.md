---
name: bank-reconciliation
description: 銀行匯款對帳技能（全域可用）。把官網後台匯出的「匯款訂單」CSV（cp950）整理進銀行匯款對帳總表：交易日（匯款日期）、存款金額（匯款金額）、摘要（轉帳後五碼，遇賣家備註寫「正確後5碼」時改用正確碼覆蓋）、備註（匯款銀行）、部門（固定電圖部）、網站訂單編號、賣家備註；並可用 --lookup 讀「客戶訂單｜客戶代號」對照檔，比對網站訂單編號後回填客戶代號。摘要自動以文字格式保留前導零。說「銀行對帳」「匯款對帳」「整理匯款訂單」「整理銀行資料表」「匯款資料整理」「比對客戶代號」「補客戶代號」時載入。
---

# 銀行匯款對帳 (bank-reconciliation)

## 用途

把官網後台（綠界／藍新金流）匯出的**匯款訂單 CSV**，整理進**銀行匯款對帳總表**（xlsx），供後續與銀行對帳單核對；並可從「客戶訂單｜客戶代號」對照檔**回填客戶代號**。

## 檔案位置

`~/.config/opencode/skills/bank-reconciliation/bank_recon.py`

## 使用方式

```powershell
# ① 匯款資料寫進總表
python ~/.config/opencode/skills/bank-reconciliation/bank_recon.py <匯款訂單.csv> [--total 對帳總表.xlsx] [--dept 電圖部]

# ② 再補客戶代號（同一次做完）
python ~/.config/opencode/skills/bank-reconciliation/bank_recon.py <匯款訂單.csv> --lookup "☺客戶訂單+代號_銷貨明細 -_YYYYMMDD.xlsx"

# ③ 只補客戶代號（總表已有資料，不想重跑 CSV）
python ~/.config/opencode/skills/bank-reconciliation/bank_recon.py --lookup "☺客戶訂單+代號_銷貨明細 -_YYYYMMDD.xlsx"
```

### 參數

| 參數 | 說明 |
|------|------|
| `匯款訂單.csv` | 官網匯出的匯款訂單 CSV（cp950 / Big5）。可省略，省略時只做 `--lookup` |
| `--total` | 對帳總表檔名（預設 `銀行匯款對帳資料-總表.xlsx`），**不存在會自動建立** |
| `--dept` | 部門固定值（預設 `電圖部`） |
| `--lookup` | 「客戶訂單｜客戶代號」兩欄 xlsx，用來比對回填客戶代號 |
| `--overwrite` | 清空總表既有資料列後重寫（只保留表頭） |

> `--lookup` 若在還沒建立表格前就給，會自動在「網站訂單編號」右邊插入「客戶代號」欄；重複執行不會重複插欄。

---

## 總表欄位

| 欄 | 名稱 | 備註 |
|----|------|------|
| A | 交易日 | |
| B | 存款金額 | |
| C | 摘要 | |
| D | 備註 | |
| E | 部門 | |
| F | 網站訂單編號 | |
| G | **客戶代號** | 有 `--lookup` 才會出現（插在網站訂單編號右邊） |
| H | 賣家備註 | |

## 欄位對應規則（定案）

| 總表欄位 | 來源／規則 |
|----------|-----------|
| 交易日 | 官網「匯款日期」（`YYYY-MM-DD`），輸出為日期格式 `yyyy/mm/dd`；未匯款者留空 |
| 存款金額 | 官網「匯款金額」（數字格式 `#,##0`）；未匯款者為 0 |
| 摘要 | 官網「轉帳後五碼」；**若「賣家備註」含「正確後5碼」等字樣，改用該 5 位數字覆蓋**。一律存成**文字格式**（保留 `00780`、`08986` 前導零） |
| 備註 | 官網「匯款銀行」原文（例：`中國信託`、`中華郵政`、`013`、`郵局`） |
| 部門 | 固定值（預設 `電圖部`） |
| 網站訂單編號 | 官網「訂單編號」（E 開頭，例：`E2609080034`） |
| 客戶代號 | 由 `--lookup` 對照檔比對：以總表「網站訂單編號」對照檔案「客戶訂單」，帶入「客戶代號」（例 `E2609080034 → W901248503`） |
| 賣家備註 | 官網「賣家備註」原文 |

> 來源 CSV 沒有的欄位一律**保留空白**。

---

## 處理流程

1. **讀取 CSV**：依序嘗試 `cp950` → `big5` → `utf-8-sig` → `utf-8`，自動判斷編碼
2. **刪除全空白列**
3. **逐列對應欄位**（規則如上表）
4. **摘要修正**：掃描「賣家備註」，若有「正確」字樣則抓出第一個 5 位數字覆蓋摘要
   - 例：`轉帳後五碼=35538`、`賣家備註=正確後5碼06306` → 摘要輸出 `06306`
5. **寫入總表**：讀入既有總表 → 於最後一列之後**接續寫入**（表頭與既有資料、參考列都保留）
6. **客戶代號回填**（有 `--lookup` 時）：
   - 讀 `--lookup` 檔第一張工作表，取 A 欄「客戶訂單」、B 欄「客戶代號」建立去重字典
   - 確保總表有「客戶代號」欄（沒有就插在「網站訂單編號」右邊）
   - 逐列以「網站訂單編號」查表，命中就帶入；印出 `帶入 N/M 列` 與未命中清單
7. **格式化**：交易日置中＋日期格式、存款金額靠右＋千分位、摘要文字格式＋置中

---

## 輸出範例

```
來源: E-0922.csv（編碼 cp950）
寫入 20 筆 → 銀行匯款對帳資料-總表.xlsx（第 3~22 列）
摘要以「正確後5碼」覆蓋: 2 筆
客戶代號比對: 查找表 1205 筆（衝突 0）→ 帶入 21/21 列
```

---

## 注意事項

1. **來源 CSV 別用 Excel 開啟後另存**：12 位數的單號（如出貨單號）會被 Excel 轉成科學記號 `9.07873E+11`，末幾碼永久遺失。要改請用文字編輯器或直接重新匯出。
2. **摘要是文字格式**：即使 `24768` 這種沒有前導零的值也存成文字，避免 Excel 自動轉數字。
3. **總表可累積**：每次執行都接在最後一列之後，適合每天把新一批匯款資料併入同一張總表。
4. **未匯款列**（匯款方式＝未選擇匯款方式、匯款金額 0）：交易日／摘要／備註留空、存款金額 0，需人工確認。
5. **客戶代號對照檔**：`--lookup` 檔案第一張工作表須為兩欄（A=客戶訂單、B=客戶代號），從第 2 列起為資料；同一客戶訂單有兩種代號時會列入衝突並以最後一筆為準。
6. 官網 CSV 欄位名若改版，需同步調整 `bank_recon.py`。

---

## 依賴

- `openpyxl`

```powershell
pip install openpyxl
```
